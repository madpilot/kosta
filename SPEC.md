# Gardening Assistant - Specification Document

## Project Overview

- **Project Name**: Garden Agent
- **Type**: TypeScript API Application with AI capabilities
- **Core Functionality**: A gardening assistant that manages plants, provides planting/maintenance calendars with AI-assisted scheduling, and offers gardening advice through an AI agent.
- **Target Users**: Home gardeners who want to track their garden and get AI-powered gardening advice.

## Technical Architecture

### Stack

- **Language**: TypeScript
- **API Framework**: oRPC (with OpenAPI schema generation)
- **Database**: SQLite (default), PostgreSQL (production)
- **Testing**: Jest + Fishery (factories)
- **Linting**: Airbnb ESLint rules
- **Type Checking**: TypeScript (tsc)

### Key Dependencies

- `better-sqlite3` for SQLite datastore
- `pg` for PostgreSQL
- `zod` for schema validation
- `@orpc/server` for API
- `ollama` for local AI models
- `jose` / `ai` for multi-model AI support

## Feature Specification

### 1. Plant Database

Users can manage their personal plant collection.

#### Data Model: Plant

- `id`: UUID
- `name`: String (common name)
- `species`: String
- `location`: String (garden bed/zone)
- `plantedDate`: Date
- `lastWatered`: Date
- `wateringFrequency`: Number (days between watering)
- `lastFertilized`: Date
- `fertilizingFrequency`: Number (days between fertilizing)
- `notes`: Text
- `sunlightRequirement`: Enum (full-sun, partial-shade, shade)
- `soilType`: String
- `harvestDate`: Date (optional)
- `createdAt`: Timestamp
- `updatedAt`: Timestamp

#### API Endpoints

- `GET /plants` - List all plants
- `GET /plants/:id` - Get plant details
- `POST /plants` - Add new plant
- `PUT /plants/:id` - Update plant
- `DELETE /plants/:id` - Remove plant

### 2. Calendar System

Automated planting and maintenance scheduling.

#### Data Model: CalendarEvent

- `id`: UUID
- `plantId`: UUID (FK)
- `type`: Enum (watering, fertilizing, harvesting, pruning, planting)
- `scheduledDate`: Date
- `completed`: Boolean
- `completedAt`: Date
- `notes`: Text
- `autoGenerated`: Boolean (AI-generated vs manual)
- `createdAt`: Timestamp

#### API Endpoints

- `GET /calendar` - List events (with date range filters)
- `POST /calendar` - Create manual event
- `PUT /calendar/:id` - Update event
- `DELETE /calendar/:id` - Delete event
- `POST /calendar/generate` - AI generates schedule
- `GET /calendar/ical` - Export as iCal feed

#### AI Calendar Generation

- Uses plant database info + location + weather + season
- Integrates with weather API for precipitation data
- Considers local frost dates for planting

### 3. iCal Feed

- Exposes calendar events as iCal format
- Supports URL parameter for date range
- Includes proper VEVENT format

### 4. AI Agent

Multi-model AI chat for gardening advice.

#### Data Model: ChatMessage

- `id`: UUID
- `role`: Enum (user, assistant)
- `content`: Text
- `model`: String
- `timestamp`: Timestamp

#### Data Model: ChatSession

- `id`: UUID
- `userId`: String
- `messages`: ChatMessage[]
- `createdAt`: Timestamp
- `updatedAt`: Timestamp

#### API Endpoints

- `POST /chat` - Send message to AI
- `GET /chat/sessions` - List chat sessions
- `GET /chat/sessions/:id` - Get session history
- `DELETE /chat/sessions/:id` - Delete session
- `GET /models` - List available AI models

#### Supported Models

- OpenAI GPT models (configurable)
- Local Ollama models
- Anthropic Claude (optional)

## Database Schema

### SQLite (default)

Location: `data/garden.db`

### PostgreSQL (production)

Connection via `DATABASE_URL` env var

## Project Structure

```
/
├── src/
│   ├── index.ts              # Entry point
│   ├── api/                 # oRPC handlers
│   │   ├── plants.ts
│   │   ├── calendar.ts
│   │   ├── chat.ts
│   │   └── index.ts
│   ├── db/                  # Database
│   │   ├── index.ts
│   │   ├── sqlite.ts
│   │   └── postgres.ts
│   ├── models/               # Zod schemas
│   │   ├── plant.ts
│   │   ├── calendar.ts
│   │   └── chat.ts
│   ├── services/            # Business logic
│   │   ├── plants.ts
│   │   ├── calendar.ts
│   │   ├── ai.ts
│   │   └── ical.ts
│   └── utils/               # Utilities
│   ├── *.test.ts            # Test files placed with source
│   └── *.test.tsx
├── data/
├── eslint.config.js
├── tsconfig.json
├── package.json
└── jest.config.js
```

## PR Phasing

1. **Project Setup**: Package.json, TypeScript, ESLint, Jest config
2. **Database Layer**: SQLite/PostgreSQL connection, migrations
3. **Plant CRUD API**: Full plant management with Zod validation
4. **Calendar CRUD API**: Event management
5. **iCal Export**: Calendar feed endpoint
6. **AI Chat Service**: Multi-model support with Ollama
7. **AI Calendar Generation**: Smart scheduling
8. **OpenAPI Schema**: Comprehensive API documentation
9. **Testing Suite**: Test files colocated with source files

## Acceptance Criteria

- [ ] TypeScript compiles without errors
- [ ] All endpoints use Zod validation
- [ ] Airbnb linting passes
- [ ] Jest tests pass with >80% coverage
- [ ] SQLite works out of the box
- [ ] PostgreSQL works with DATABASE_URL
- [ ] iCal feed returns valid .ics format
- [ ] AI chat works with OpenAI and Ollama
- [ ] OpenAPI schema generated at /api/docs
- [ ] Every PR feature-complete and tested
