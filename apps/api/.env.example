# Server
PORT=3000
HOST=0.0.0.0

# Database (default: ./data/garden.db).
# Required to bootstrap the SQLite file location — the rest of the app's
# configuration (AI backend, model, weather key, location, hemisphere,
# logging, etc.) is stored inside this database and managed via
# /api/onboarding (first run) and /api/settings (afterwards).
DATABASE_URL=
